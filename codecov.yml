codecov:
  require_ci_to_pass: true

coverage:
  range: "50...100"
  status:
    project:
      default:
        target: 50%
        threshold: 1%
        if_ci_failed: error
    patch:
      default:
        target: 50%
