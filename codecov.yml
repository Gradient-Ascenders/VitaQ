codecov:
  require_ci_to_pass: true

coverage:
  range: "60...100"
  status:
    project:
      default:
        target: 60%
        threshold: 1%
        if_ci_failed: error
    patch:
      default:
        target: 60%
